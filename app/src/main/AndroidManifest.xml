<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_rounded"
        android:supportsRtl="true"
        android:theme="@style/Theme.GelKurye"
        tools:targetApi="31">
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Screens.OnBoard.OnBoard1" />
        <activity android:name=".Screens.OnBoard.OnBoard2" />
        <activity android:name=".Screens.OnBoard.OnBoard3" />
        <activity android:name=".Screens.MainActivity" />
        <activity android:name=".Screens.Authentication.Login" />
        <activity android:name=".Screens.Authentication.Register" />
        <activity android:name=".Screens.General.Welcome" />
    </application>

</manifest>